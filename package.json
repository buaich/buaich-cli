{
  "name": "buaich-cli",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "publish": "changeset publish",
    "ci:publish": "pnpm -r publish --provenance --access public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
